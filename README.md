# LKMhook

kernel hook library for no-source kernel programming: injected symbol
resolution, fixmap writes to read-only memory, tracked function pointer
replacement, kprobe/kretprobe wrappers, ARM64 GKI.
